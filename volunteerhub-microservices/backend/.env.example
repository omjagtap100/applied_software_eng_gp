PORT=5000
# Use either:
# - a standard (non-SRV) Mongo URI from Atlas (recommended if SRV DNS is blocked), OR
# - an SRV URI (mongodb+srv://...) if SRV lookups work in Node on your machine
MONGO_URI="mongodb+srv://omjagtap3304_db_user:bu24sMMXlXo5jO8G@cluster0.zyrfzy8.mongodb.net/volunteerhubdb"

# Optional: fix Node DNS SRV failures when Node is using a local DNS resolver (e.g. 127.0.0.1)
# Example values: 1.1.1.1,1.0.0.1  (Cloudflare)  OR  8.8.8.8,8.8.4.4 (Google)
DNS_SERVERS=1.1.1.1,1.0.0.1
